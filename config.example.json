{
  "trueSota": {
    "baseUrl": "https://true-sota.com",
    "accountProbe": {
      "env": "TRUE_SOTA_WEB_TOKEN",
      "profileEndpoint": "/api/v1/user/profile",
      "errorsEndpoint": "/api/v1/usage/errors?page=1&page_size=20",
      "auth": "Authorization: Bearer <explicit-account-or-monitor-token>",
      "note": "Prefer the in-app connection panel. The token is saved to the OS credential store; this env var is only a local override."
    }
  }
}
